# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.lock.hcl

# Variables (may contain secrets)
*.tfvars
*.tfvars.json
!example.tfvars

.env

# Temporary files and downloaded binaries
get_helm.sh
kubectl
ubprocess, json
sts get-caller-identity --output json